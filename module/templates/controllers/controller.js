define(['app/app'], function(app){
	console.group('Chargement du controleur <%= nameModule %>Controller');

	app.controller('<%= nameModule %>Controller', function ($scope, $http) {
		
	});

	console.groupEnd();
});