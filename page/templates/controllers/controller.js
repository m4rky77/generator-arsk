define(['app/app'], function(app){
	
	console.group('Chargement du controleur <%= namePage %>Controller');
	app.controller('<%= namePage %>Controller', function ($scope, $http) {
		
	});

	console.groupEnd();
});